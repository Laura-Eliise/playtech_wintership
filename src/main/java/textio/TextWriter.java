package textio;

public class TextWriter {
    /// Creates a Text file from [data] and write it to [path].
    /// If the file can't be written to [path], an error will be thrown.
    public void write(String path, String data) {

    }
}
